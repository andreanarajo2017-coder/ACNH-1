// Domain tables (users, tasks, events, ...) land in M2 — Dominio base (see
// spec section 6 and docs/milestones.md). Kept empty in M0 so drizzle-kit
// has a valid entry point from the start.
export {};
